function [Salt,Temp,Deep] = readData(filename,latminp,latmaxp,logminp,logmaxp,NS,EW)
------数据读取函数------
-   输入数据：
        year
        month
        latminp
        latmaxp
        logminp
        logmaxp
        NS
        EW
-   输出数据
        某点

[latmin,latmax,logmin,logmax]=tran(latminp,latmaxp,logminp,logmaxp,NS,EW);
file_dir = strcat('..\\DATA\\全球海洋Argo网格数据集\\',filename);
load(file_dir);
Salt=salt(logmin:logmax,latmin:latmax,:);
Temp=temp(logmin:logmax,latmin:latmax,:);
Deep=pres;
end